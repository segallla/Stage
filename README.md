# Stage

This repository provides a simple BESS pipeline example.

## Usage

```python
from bess.pipeline import is_feasible

address = "123 Example St, City, ST 00000"
print(is_feasible(address))
address2 = "5300 Sheila St, Commerce, CA 90040"
print(is_feasible(address2))
```
